package com.DSA.TimeComplexity;

public class intro {
    // O(n)[ big oh] = it simply means that a code will have at max this time complexity.
    //omega(n)[big omega] = on other hand it means that code will at least will take n complexity.
    //theta(n) [big theta] = it simply means that combination of least and max.
// o(n) little oh = strictly maximum bound;
    // vice versa to omega little

}
